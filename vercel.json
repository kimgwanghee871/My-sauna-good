{
  "ignoreCommand": "if [ \"$VERCEL_ENV\" = \"production\" ] && [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then echo 'Ignore non-main production deployment'; exit 0; else echo 'Proceeding with deployment'; fi"
}